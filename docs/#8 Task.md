# Task Tracker — Windows Local Testing Fix

- [x] Changed backend default port from `3000` to `3001`.
- [x] Changed backend `.env.example` to `PORT=3001`.
- [x] Changed mobile API fallback port to `3001`.
- [x] Added `EXPO_PUBLIC_API_PORT` support for mobile development.
- [x] Added `react-dom` and `react-native-web` to mobile dependencies for web testing.
- [x] Aligned Expo package versions for `expo-location` and `react-native-maps` in `package.json`.
- [x] Updated README with Windows two-terminal startup instructions and API override examples.
